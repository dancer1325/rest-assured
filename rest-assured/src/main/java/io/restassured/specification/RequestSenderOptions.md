* == options available | sending a request
* `String path` | ALL methods
  * ways to specify the path
    * fully-qualified -- "https:domain:path" --
    * just path -- "path" --
      * -> 👁️ == "http://localhost:8080/path" 👁️
* TODO: